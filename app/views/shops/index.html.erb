
<div class='row pt-5'>
<div class='col-3' style="background-color: :red">
  <%= render 'shared/search' %>
  <%= render 'categories/category' %>
  <%= render 'shared/range_price' %>
</div>
<div class='col-9'>
  <div class="row row-cols-4">
    <% @products.each do |item| %>
    
    <div class="col">
      <div class="card">
        <img src='https://dummyimage.com/640x360/fff/aaa' class="card-img-top" >
          <div class="card-body">
            <h5 class="card-title"><%= item.name %></h5>
            <p class="card-text"><%= item.description %></p>
          </div>
          <div class="card-footer">
            <small class="text-body-secondary"><%= number_to_currency(item.price, precision: 2, unit: 'UAH ') %></small>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
</div>





<%= link_to 'New product', new_product_path %>

