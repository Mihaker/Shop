<ul class="list-group list-group-flush pt-4">
  <p class='fs-3'>Категорії</p>
  <% @categories.each do |item| %>
   <%= link_to item.name, category_path(item), class: 'list-group-item link-info link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover' %>
  <% end %>
</ul>
